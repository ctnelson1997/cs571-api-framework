export interface CS571DefaultPublicConfig {
    readonly SESSION_LENGTH: number;
    readonly TIMEOUT_WINDOW_LENGTH: number;
    readonly TIMEOUT_WINDOW_REQS: number;
    readonly SKIP_LOGGING?: boolean;
    readonly LOG_IPS: boolean;
}